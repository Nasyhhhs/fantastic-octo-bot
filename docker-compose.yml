version: "3.9"

services:
  telegram-bot:
    container_name: telegram-bot
    working_dir: /app
    env_file:
      - ./.env
    restart: on-failure
    build:
        context: .
        dockerfile: Dockerfile
    command: sh -c "python main.py"